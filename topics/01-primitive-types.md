# Примитивные типы данных в Go

> Короткий ответ (на собесе): В Go примитивные типы данных делятся на числовые, логический и строковый. Числовые включают целочисленные типы (int, uint, типы фиксированной разрядности), вещественные (float32, float64) и комплексные числа с вещественной и мнимой частью (complex64, complex128). Логический тип — bool. Строковый тип — string, строки в Go иммутабельны и хранятся в UTF-8. Также используется тип rune, который является int32 и представляет Unicode-кодпоинт.

---

## 📚 Развёрнутый ответ

**Числовые типы**  
Целочисленные: int, uint, int8–int64, uint8–uint64  
Вещественные: float32, float64  
Комплексные: complex64, complex128

**Логический тип**  
bool — хранит true и false, не поддерживает неявные преобразования.

**Строки**  
string — иммутабельный тип, хранит данные в UTF-8.  
len(string) возвращает количество байт, а не символов.

**Rune**  
rune — это int32, представляющий Unicode-кодпоинт.

---

## ⚠️ Важно помнить

- В Go нет типа char  
- string нельзя изменить по индексу  
- rune — это число  
- len("привет") == 12

---

## 💡 Пример

```go
s := "привет"
fmt.Println(len(s))        // 12
fmt.Println(len([]rune(s))) // 6
