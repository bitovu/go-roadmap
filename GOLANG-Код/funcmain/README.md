# ▶️ GOLANG — `func main`

---

## 1. Что такое `func main`

> **`func main` — это точка входа Go-программы.**

Именно с неё начинается выполнение программы.

Без `func main`:

* программу нельзя запустить
* `go run` / `go build` выдадут ошибку

---

## 2. Пакет `main`

`func main` **обязательно должна находиться в пакете `main`**.

```go
package main

func main() {
    // старт программы
}
```

Важно:

* `package main` ≠ имя файла
* это специальный пакет для исполняемых программ

---

## 3. Что происходит при запуске программы

Когда ты делаешь:

```bash
go run .
go build
./app
```

Происходит:

1. ОС запускает бинарник
2. управление получает **Go runtime**
3. runtime инициализирует:

   * память
   * scheduler
   * GC
4. runtime вызывает **`main.main()`**

👉 `func main` вызывается **не напрямую ОС**, а через runtime.

---

## 4. Почему `main` вызывается один раз

* `func main` вызывается **ровно один раз**
* после выхода из `main`:

  * программа завершается
  * все горутины останавливаются

```go
func main() {
    fmt.Println("end")
}
// программа завершится здесь
```

---

## 5. Горутины и `main`

Если `main` завершилась — программа **закрывается**, даже если:

* другие горутины ещё работают

```go
func main() {
    go fmt.Println("hello")
}
// hello может не вывестись
```

Правильно:

```go
func main() {
    var wg sync.WaitGroup
    wg.Add(1)

    go func() {
        defer wg.Done()
        fmt.Println("hello")
    }()

    wg.Wait()
}
```

---

## 6. `init()` и `main()`

Перед `main` вызываются все функции `init()`:

```go
func init() {
    fmt.Println("init")
}

func main() {
    fmt.Println("main")
}
```

Порядок:

1. `init()` из импортируемых пакетов
2. `init()` текущего пакета
3. `main()`

---

## 7. Аргументы командной строки

`func main` **не принимает аргументы**, но их можно получить:

```go
import "os"

func main() {
    args := os.Args
}
```

---

## 8. Что нельзя делать с `main`

* ❌ нельзя вызывать `main()` вручную
* ❌ нельзя иметь несколько `main` в одном пакете
* ❌ нельзя вернуть значение

```go
func main() int {} // ❌
```

---

## 9. `main` и тесты

* `func main` **не вызывается** при `go test`
* тесты используют свой runtime-вход

---

## 10. Короткий конспект

* `func main` — точка входа
* только в `package main`
* вызывается runtime
* вызывается один раз
* завершение `main` = завершение программы

---

## 11. Главная мысль

> **`func main` — это не просто функция, а контракт между Go runtime и твоей программой.**

---

