# 🌐 GOLANG — net/http

Пакет `net/http` — это **стандартный HTTP‑сервер и клиент в Go**.

---

## 1. Что такое `net/http`

> **`net/http` позволяет писать HTTP‑серверы и клиенты без сторонних библиотек.**

Особенности:

* входит в стандартную библиотеку
* простой API
* каждый запрос обрабатывается в отдельной горутине

---

## 2. Простейший HTTP‑сервер

```go
http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
    w.Write([]byte("hello"))
})

http.ListenAndServe(":8080", nil)
```

* `HandleFunc` — регистрирует обработчик
* `ListenAndServe` — запускает сервер

---

## 3. Handler

Handler — это функция:

```go
func(w http.ResponseWriter, r *http.Request)
```

* `ResponseWriter` — ответ клиенту
* `Request` — входящий запрос

---

## 4. HTTP‑методы

```go
r.Method == http.MethodGet
r.Method == http.MethodPost
```

Часто проверяются вручную.

---

## 5. Чтение параметров запроса

### Query‑параметры

```go
name := r.URL.Query().Get("name")
```

### Тело запроса

```go
body, _ := io.ReadAll(r.Body)
```

---

## 6. Ответ клиенту

```go
w.WriteHeader(http.StatusOK)
w.Write([]byte("ok"))
```

Для JSON:

```go
w.Header().Set("Content-Type", "application/json")
```

---

## 7. HTTP‑клиент

```go
resp, err := http.Get("https://example.com")
if err != nil { }
defer resp.Body.Close()
```

---

## 8. Конкурентность

* каждый запрос = отдельная горутина
* `net/http` сам управляет параллелизмом

Будь аккуратен с общими данными.

---

## 9. Типичные ошибки

* ❌ не закрывать `r.Body`
* ❌ игнорировать ошибки
* ❌ писать логи через `fmt`

---

## 10. Короткий конспект

* `net/http` — стандартный HTTP
* handler = функция
* каждый запрос в своей горутине

---

## 11. Главная мысль

> **`net/http` достаточно мощный для большинства сервисов без фреймворков.**

---

