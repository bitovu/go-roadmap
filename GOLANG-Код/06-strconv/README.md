# 🔁 GOLANG — strconv

Пакет `strconv` используется для **преобразования строк в числа и обратно**.

---

## 1. Зачем нужен `strconv`

> **`strconv` нужен, когда данные приходят в виде строк, а работать нужно с числами.**

Типичные случаи:

* ввод пользователя
* данные из HTTP-запросов
* конфиги
* env-переменные

--- 

## Короткий конспект

* `Atoi` — string → int
* `ParseInt` — string → int64
* `ParseFloat` — string → float
* `Itoa` — int → string

---

## 2. Строка → int

```go
i, err := strconv.Atoi("123")
if err != nil {
    // ошибка преобразования
}
```

* `Atoi` = ASCII to int
* работает только с `int`

---

## 3. Строка → int (расширенно)

```go
i64, err := strconv.ParseInt("123", 10, 64)
```

Параметры:

* строка
* основание системы счисления
* размер бита (8, 16, 32, 64)

---

## 4. Строка → float

```go
f, err := strconv.ParseFloat("3.14", 64)
```

---

## 5. Строка → bool

```go
b, err := strconv.ParseBool("true")
```

Поддерживаемые значения:

* `1`, `t`, `T`, `TRUE`, `true`
* `0`, `f`, `FALSE`, `false`

---

## 6. Число → строка

```go
s1 := strconv.Itoa(42)
s2 := strconv.FormatInt(42, 10)
s3 := strconv.FormatFloat(3.14, 'f', 2, 64)
```

---

## 7. Частые ошибки

* ❌ игнорировать `err`
* ❌ использовать `Atoi` для `int64`
* ❌ парсить без проверки формата

---

## 8. Когда использовать

* входные данные = строки
* нужны безопасные преобразования
* нельзя доверять формату

---


## 10. Главная мысль

> **`strconv` — это безопасный мост между строками и числами.**

---


* `strings`
