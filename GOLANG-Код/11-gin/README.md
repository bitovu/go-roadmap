# 🧪 GOLANG — gin

`gin` — это **популярный HTTP-фреймворк для Go**, построенный поверх `net/http`.

---

## 1. Что такое gin

> **gin упрощает создание HTTP API и веб-сервисов.**

Он предоставляет:

* удобный роутинг
* middleware
* JSON из коробки
* хорошую производительность

---

## 2. Установка

```bash
go get -u github.com/gin-gonic/gin
```

---

## 3. Минимальный сервер

```go
r := gin.Default()

r.GET("/", func(c *gin.Context) {
    c.JSON(200, gin.H{"message": "ok"})
})

r.Run(":8080")
```

* `gin.Default()` — router + middleware
* `Run()` — запуск сервера

---

## 4. Контекст `*gin.Context`

`Context` — ключевая сущность в gin.

Он содержит:

* запрос
* ответ
* параметры
* методы для JSON, status, headers

---

## 5. Роутинг

```go
r.GET("/users", handler)
r.POST("/users", handler)
r.PUT("/users/:id", handler)
r.DELETE("/users/:id", handler)
```

Параметры:

```go
id := c.Param("id")
```

---

## 6. Query и Body

### Query

```go
name := c.Query("name")
```

### JSON body

```go
var req Request
c.BindJSON(&req)
```

---

## 7. Middleware

Middleware — код, который выполняется **до или после handler**.

```go
r.Use(gin.Logger(), gin.Recovery())
```

Свой middleware:

```go
func Auth() gin.HandlerFunc {
    return func(c *gin.Context) {
        // check auth
        c.Next()
    }
}
```

---

## 8. Группы роутов

```go
api := r.Group("/api")
{
    api.GET("/users", handler)
}
```

---

## 9. Обработка ошибок

```go
c.AbortWithStatusJSON(400, gin.H{
    "error": "bad request",
})
```

---

## 10. Когда использовать gin

* REST API
* backend для фронта
* микросервисы

Когда **не нужен**:

* простые HTTP-хендлеры
* ultra-high performance

---

## 11. Частые ошибки

* ❌ вся логика в handler
* ❌ игнорирование middleware
* ❌ отсутствие структуры проекта

---

## 12. Короткий конспект

* gin = фреймворк над `net/http`
* `Context` — центр управления
* middleware — важнейшая часть

---

## 13. Главная мысль

> **gin позволяет быстро и аккуратно писать HTTP API на Go.**

---
