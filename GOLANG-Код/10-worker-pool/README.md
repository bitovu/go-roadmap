# 🧵 GOLANG — worker pool

Worker pool — это **паттерн конкурентности**, который позволяет **ограничить количество одновременно работающих горутин**.

---

**ПОДРОБНЕЕ В КАНАЛЫ > ПАТТЕРНЫ**

---

## 1. Зачем нужен worker pool

> **Worker pool нужен для контроля нагрузки.**

Проблема без pool:

* запускается слишком много горутин
* растёт потребление памяти
* CPU перегружается

Worker pool решает:

* ограничение параллелизма
* стабильную нагрузку
* предсказуемое поведение

---

## 2. Базовая идея

* есть **jobs** (задачи)
* есть **workers** (воркеры)
* workers читают задачи из канала

```text
jobs -> [ worker ]
     -> [ worker ]
     -> [ worker ]
```

---

## 3. Простейшая реализация

```go
jobs := make(chan int)

worker := func(id int, jobs <-chan int) {
    for job := range jobs {
        fmt.Println("worker", id, "job", job)
    }
}

for i := 0; i < 3; i++ {
    go worker(i, jobs)
}

for j := 0; j < 10; j++ {
    jobs <- j
}
close(jobs)
```

---

## 4. Worker pool + WaitGroup

```go
var wg sync.WaitGroup

worker := func(jobs <-chan int) {
    defer wg.Done()
    for job := range jobs {
        process(job)
    }
}

for i := 0; i < 5; i++ {
    wg.Add(1)
    go worker(jobs)
}

close(jobs)
wg.Wait()
```

---

## 5. Когда использовать

* CPU-bound задачи
* ограничение ресурсов
* обработка очередей
* фоновые воркеры

---

## 6. Когда НЕ нужен

* мало задач
* I/O-bound (часто хватает горутин)
* простая логика

---

## 7. Частые ошибки

* ❌ забыть закрыть канал
* ❌ запускать воркеров больше, чем нужно
* ❌ делать worker pool без причины

---

## 8. Worker pool vs просто goroutines

* goroutines — максимум параллелизма
* worker pool — **контроль** параллелизма

---

## 9. Короткий конспект

* worker pool = ограничение горутин
* канал = очередь задач
* sync.WaitGroup = ожидание

---

## 10. Главная мысль

> **Worker pool нужен не для скорости, а для стабильности.**

---
