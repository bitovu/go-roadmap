# 🖨️ GOLANG — fmt / log

Эта методичка объясняет, **как в Go выводить информацию и логировать работу программы**.

---

## 1. Пакет `fmt`

> **`fmt` — пакет для форматированного ввода и вывода.**

Используется для:

* вывода в консоль
* форматирования строк
* быстрого дебага

---

## 2. Базовый вывод

```go
fmt.Print("hello")
fmt.Println("hello")
fmt.Printf("hello %s", "world")
```

* `Print` — без перевода строки
* `Println` — с переводом строки
* `Printf` — форматированный вывод

---

## 3. Форматирование (`Printf`)

Частые спецификаторы:

* `%v` — значение
* `%+v` — структура с полями
* `%#v` — Go-представление
* `%T` — тип
* `%d` — int
* `%s` — string
* `%f` — float

```go
fmt.Printf("value=%v type=%T", x, x)
```

---

## 4. Ошибки и fmt

```go
if err != nil {
    fmt.Println("error:", err)
}
```

Для оборачивания ошибок:

```go
fmt.Errorf("open file: %w", err)
```

---

## 5. Пакет `log`

> **`log` — стандартный логгер Go.**

Особенности:

* пишет в `stderr`
* автоматически добавляет дату и время
* потокобезопасен

---

## 6. Базовое логирование

```go
log.Println("started")
log.Printf("user=%d", id)
```

---

## 7. Fatal и Panic

```go
log.Fatal("fatal error") // завершает программу
log.Panic("panic error") // вызывает panic
```

* `Fatal` → `os.Exit(1)`
* `Panic` → panic + stack trace

---

## 8. Настройка логгера

```go
log.SetPrefix("[APP] ")
log.SetFlags(log.LstdFlags | log.Lshortfile)
```

Флаги:

* `Ldate`, `Ltime`
* `Lshortfile`, `Llongfile`

---

## 9. Когда fmt, а когда log

* `fmt`:

  * CLI
  * учебные примеры
  * быстрый дебаг

* `log`:

  * серверы
  * сервисы
  * ошибки и события

---

## 10. Частые ошибки

* ❌ использовать `fmt.Println` для логов в проде
* ❌ игнорировать ошибки
* ❌ логировать слишком много

---

## 11. Короткий конспект

* `fmt` — вывод и форматирование
* `log` — логирование
* `Printf` — контроль формата
* `log.Fatal` завершает программу

---

## 12. Главная мысль

> **fmt — для вывода, log — для жизни сервиса.**

---


* `sync`
