# 🔤 GOLANG — strings

Пакет `strings` используется для **работы со строками**: поиска, модификации, разбиения и сборки.

---

## 1. Зачем нужен `strings`

> **`strings` — основной инструмент для обработки текста в Go.**

Используется в:

* HTTP
* парсинге данных
* CLI
* логике бизнес-правил

---

## 2. Поиск в строках

```go
strings.Contains(s, "go")
strings.HasPrefix(s, "http")
strings.HasSuffix(s, ".json")
```

---

## 3. Изменение регистра

```go
strings.ToLower(s)
strings.ToUpper(s)
```

---

## 4. Обрезка пробелов и символов

```go
strings.TrimSpace(s)
strings.Trim(s, "/")
strings.TrimPrefix(s, "http://")
strings.TrimSuffix(s, "/")
```

---

## 5. Разделение и объединение

```go
parts := strings.Split(s, ",")
result := strings.Join(parts, ",")
```

---

## 6. Замена подстрок

```go
strings.Replace(s, "a", "b", 1)
strings.ReplaceAll(s, " ", "-")
```

---

## 7. Повторение строк

```go
strings.Repeat("-", 10)
```

---

## 8. strings.Builder (эффективно)

`Builder` используется для **эффективной сборки строк**.

```go
var b strings.Builder
b.WriteString("hello")
b.WriteString(" world")
result := b.String()
```

Лучше, чем `+=` в цикле.

---

## 9. Частые ошибки

* ❌ конкатенация строк через `+` в циклах
* ❌ забывать про `TrimSpace`
* ❌ путать байты и руны

---

## 10. Короткий конспект

* `Contains` — поиск
* `Split / Join` — разбиение
* `Trim*` — очистка
* `Builder` — производительность

---

## 11. Главная мысль

> **`strings` — это швейцарский нож для работы с текстом.**

---
