# 📦 GOLANG — МОДУЛИ

---

## 1. Что такое модуль

> **Модуль в Go — это единица версионирования и зависимостей.**

Проще:

* модуль = проект
* модуль описывает:

  * имя проекта
  * зависимости
  * версии зависимостей

Модуль задаётся файлом **`go.mod`**.

---

## 2. Зачем нужны модули

До модулей Go использовал `GOPATH`, что было неудобно.

Модули решают:

* управление зависимостями
* версии библиотек
* воспроизводимые сборки
* независимость от GOPATH

---

## 3. Инициализация модуля

```bash
go mod init example.com/myapp
```

Что происходит:

* создаётся файл `go.mod`
* задаётся имя модуля

Пример `go.mod`:

```go
module example.com/myapp

go 1.22
```

---

## 4. Имя модуля

Имя модуля — это **путь импорта**.

Обычно:

* GitHub: `github.com/user/project`
* локально можно любое: `myapp`

Важно:

* `module` = то, что пишется в `import`

---

## 5. Структура проекта с модулем

```text
myapp/
├─ go.mod
├─ go.sum
├─ main.go
└─ internal/
```

* `go.mod` — описание модуля
* `go.sum` — контрольные суммы зависимостей

---

## 6. Файл go.sum

`go.sum`:

* хранит хэши зависимостей
* гарантирует, что версии **не подменились**
* нужен для безопасности и повторяемых сборок

Его **нельзя писать вручную**.

---

## 7. Добавление зависимостей

```go
import "github.com/gin-gonic/gin"
```

Дальше:

```bash
go mod tidy
```

Go:

* найдёт модуль
* скачает версию
* обновит `go.mod` и `go.sum`

---

## 8. go mod tidy

```bash
go mod tidy
```

Что делает:

* добавляет недостающие зависимости
* удаляет неиспользуемые
* приводит `go.mod` в порядок

👉 Используй **всегда**.

---

## 9. Версии зависимостей

В `go.mod`:

```go
require github.com/gin-gonic/gin v1.9.1
```

Go использует **SemVer**:

* `v1.2.3`
* `v2+` требует изменения пути импорта

---

## 10. replace и local модули

```go
replace example.com/lib => ../lib
```

Используется:

* для локальной разработки
* для monorepo

---

## 11. internal и экспорт

Папка `internal/`:

* пакеты внутри неё **нельзя импортировать снаружи модуля**
* используется для инкапсуляции

---

## 12. Частые ошибки

* ❌ нет `go.mod`
* ❌ неправильное имя модуля
* ❌ ручное редактирование `go.sum`
* ❌ не запускать `go mod tidy`

---

## 13. Короткий конспект

* модуль = проект
* `go.mod` — главное
* `go.sum` — безопасность
* `go mod init` — старт
* `go mod tidy` — порядок

---

## 14. Главная мысль

> **Go-модули делают сборку, зависимости и версии предсказуемыми и контролируемыми.**

---

* структура проекта
* работа с несколькими модулями
