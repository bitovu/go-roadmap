# 🗂️ GOLANG — project layout

Эта методичка объясняет, **как правильно организовывать структуру Go‑проекта**, чтобы код был читаемым, масштабируемым и поддерживаемым.

---

## 1. Зачем нужен layout

> **Layout — это договорённость о том, где что лежит в проекте.**

Хороший layout:

* упрощает навигацию
* облегчает поддержку
* помогает новым разработчикам
* масштабируется без боли

---

## 2. Минимальный layout

Для небольшого проекта:

```text
myapp/
├─ go.mod
├─ main.go
└─ internal/
```

Подходит для pet‑проектов и утилит.

---

## 3. Канонический layout Go

Самый распространённый вариант:

```text
myapp/
├─ cmd/
│  └─ app/
│     └─ main.go
├─ internal/
│  ├─ handler/
│  ├─ service/
│  └─ repository/
├─ pkg/
├─ configs/
├─ migrations/
├─ go.mod
└─ go.sum
```

---

## 4. Папка cmd/

> **`cmd/` — точки входа приложения.**

* каждый подкаталог = отдельный бинарник
* внутри всегда `main.go`

Пример:

```text
cmd/api/main.go
cmd/worker/main.go
```

---

## 5. Папка internal/

> **Код, доступный только внутри модуля.**

* нельзя импортировать извне
* используется для бизнес‑логики

Это основной рабочий код приложения.

---

## 6. Папка pkg/

> **Публичные пакеты, которые могут использовать другие проекты.**

Используй `pkg/` только если код реально переиспользуем.

---

## 7. Configs, migrations и прочее

Часто встречаются:

* `configs/` — конфиги
* `migrations/` — миграции БД
* `scripts/` — скрипты
* `docs/` — документация

---

## 8. Чего делать не стоит

* ❌ всё класть в один пакет
* ❌ делать огромный `main.go`
* ❌ путать `internal` и `pkg`

---

## 9. Как делить код внутри internal

Рекомендуемый подход:

```text
internal/
├─ handler/
├─ service/
├─ repository/
├─ model/
└─ transport/
```

По слоям ответственности.

---

## 10. Короткий конспект

* layout = структура проекта
* `cmd` — точки входа
* `internal` — приватный код
* `pkg` — публичный код

---

## 11. Главная мысль

> **Хороший layout — это когда код легко найти и легко изменить.**

---
