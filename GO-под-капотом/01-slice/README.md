# 🧩 Срезы (Slices) в Go

---

## 🗣️ Ответ на собеседовании

**Срез в Go — это структура, которая ссылается на массив  и хранит указатель на данные, длину и ёмкость.
При `append` элементы добавляются в существующий массив,  если хватает ёмкости, иначе создаётся новый массив.**

---

## 📦 Что такое срез

Срез — это **не массив**.

Срез — это структура, которая содержит:
- указатель на массив данных
- длину (`len`)
- ёмкость (`cap`)

Логически (упрощённо):

```go
slice = (указатель, len, cap)
```

## 🧠 Связь с массивом
```go
arr := [5]int{1, 2, 3, 4, 5}
s := arr[1:3]

arr: [1 2 3 4 5]
      ↑   ↑
      s[0] s[1]
```
`len(s) == 2
cap(s) == 4`
-Несколько срезов могут ссылаться на один и тот же массив.

## ⚠️ Общий массив — общие изменения
```go
a := []int{1, 2, 3}
b := a[:2]

b[0] = 100
fmt.Println(a) // [100 2 3]
```
-Изменения через один срез видны в другом.

## ➕ Как работает append
✔️ Ёмкости хватает
```go
s := make([]int, 0, 5)
s = append(s, 1)
```
- используется тот же массив
-len увеличивается
-[⬅️ На главную](../)cap не меняется

❗ Ёмкости НЕ хватает
```go
s := []int{1, 2, 3}
s = append(s, 4)
```
Происходит:
* создаётся новый массив
* данные копируются
* срез начинает ссылаться на новый массив
- Старый массив остаётся, если на него есть ссылки.

## 📈 Рост ёмкости

- Go увеличивает cap не на 1.

Общая логика:

- маленькие срезы → рост примерно в 2 раза
- большие срезы → рост медленнее

⚠️ Точный алгоритм — деталь реализации.

## ❗ Важный эффект append
```go
a := []int{1, 2, 3}
b := append(a, 4)
```
Возможны два варианта:
- a и b используют один массив
- b указывает на новый массив
👉 Нельзя полагаться на то, будет ли реаллокация.

## 🧱 Copy и изоляция данных

Если нужно гарантировать независимость данных:
```go
dst := make([]int, len(src))
copy(dst, src)
```

🧭 Зачем заранее задавать ёмкость
```go
s := make([]int, 0, 1000)
```
Плюсы:
- меньше аллокаций
- быстрее append
- более предсказуемое поведение

## ⚠️ Важные нюансы (Pro Tips)

- Срез — это структура, а не массив
- Передача среза копирует только указатель, len и cap
- Несколько срезов могут ссылаться на один массив
- append может изменить базовый массив
- append может привести к созданию нового массива
- cap влияет на производительность

## 🧠 Итог

- Slice = указатель + len + cap
- append = либо тот же массив, либо новый
- Общий массив → общие изменения

---

[⬅️ домой,уолтер](../)
