# Go: Интерфейсы

---

## 1. Что такое интерфейс

**Интерфейс** в Go — это набор методов, который описывает поведение, а не реализацию.

```go
type Reader interface {
    Read(p []byte) (n int, err error)
}
```
* Интерфейс говорит ЧТО объект умеет делать, но не КАК.

---

# Go: Интерфейсы — методичка (с пункта 2)

---

## 2. Зачем нужны интерфейсы

Интерфейсы нужны для:
- абстракции
- уменьшения связности кода
- полиморфизма
- тестирования (моки)
- написания библиотек

Интерфейсы позволяют писать код, который зависит от **поведения**, а не от конкретной реализации.

Фраза для собеса:
> Интерфейсы позволяют отделить использование от реализации.

---

## 3. Неявная реализация интерфейсов (Duck typing)

В Go тип реализует интерфейс **неявно** — если у него есть все методы интерфейса.

```go
type Writer interface {
    Write([]byte) (int, error)
}

type File struct {}

func (f File) Write(p []byte) (int, error) {
    return len(p), nil
}
```

---

## Где правильно объявлять интерфейсы

- Интерфейсы объявляют со стороны потребителя, а не реализации.

---

## Type assertion

Type assertion позволяет получить конкретный тип из интерфейса.
```go
v, ok := i.(int)

ok == true — приведение успешно

ok == false — паники нет
```
Без проверки:
```go
v := i.(int) // panic, если тип не совпадает
```

---

## Type switch

Type switch используется для обработки разных типов внутри интерфейса.
```go
switch v := i.(type) {
case int:
    fmt.Println(v)
case string:
    fmt.Println(v)
default:
    fmt.Println("unknown")
}
```

---

##  Что такое `nil` в контексте интерфейсов

`nil` для интерфейсов — это **не просто отсутствие значения**.

Интерфейс в Go **состоит из двух частей**:
- типа
- значения

👉 Интерфейс равен `nil` **только если и тип, и значение равны `nil`**.

### ✅ Nil интерфейс
```go
var err error
fmt.Println(err == nil) // true
```
Здесь:
- тип = `nil`
- значение = `nil`
  
---

Логически любой интерфейс — это (type, value), но под капотом empty interface и non-empty interface представлены разными структурами: eface и iface.

