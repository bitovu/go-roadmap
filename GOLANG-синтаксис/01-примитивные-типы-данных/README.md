# Примитивные типы данных в Go

---

## 🗣️ Ответ на собеседовании (кратко)

«В Go примитивные типы данных делятся на четыре группы: **числовые**, **строковые**, **логические** и **псевдонимы**.

1. **Числовые**: целые числа (`int`, `int8-64`), вещественные (`float32/64`) и комплексные.
2. **Строковые (`string`)**: неизменяемая последовательность байт, обычно в UTF-8.
3. **Логические (`bool`)**: только `true` или `false`.
4. **Псевдонимы (Aliases)**: `byte` (uint8) для данных и `rune` (int32) для Unicode-символов.

Ключевые фишки: **строгая типизация** (нет неявных приведений) и **Zero Values** (гарантированные значения по умолчанию вместо мусора в памяти)».

---

## 🔢 Числовые типы

| Тип | Размер | Описание |
| :--- | :--- | :--- |
| `int` / `uint` | 32/64 бит | Платформо-зависимые (зависят от разрядности ОС) |
| `int8` .. `int64` | Фикс. | Числа со строго заданным размером в битах |
| `float32`, `float64` | 4 / 8 байт | Числа с плавающей точкой (IEEE-754) |
| `complex64/128` | 8 / 16 байт | Комплексные числа (состоят из двух float) |

---

## 🧵 Строки и Руны (Strings & Runes)

Это самая важная тема, на которой часто "засыпаются" новички.

### String (Строка)
* **Иммутабельность:** Строки нельзя изменять. Любое "изменение" — это создание новой строки.
* **Хранение:** Это массив байт.
* **Длина:** `len(s)` возвращает количество **байт**, а не символов. 
  * *Пример:* `len("Go")` == 2, но `len("Прив")` == 8 (в UTF-8 кириллица занимает 2 байта).

### Rune (Руна)
* **Тип:** Псевдоним для `int32`. Всегда занимает **4 байта**.
* **Зачем нужна:** Хранит один полноценный символ Unicode (Code Point).
* **Итерация:** * Если идти по строке через `for i`, получим байты. 
  * Если через `for range`, Go автоматически выдаст руны.

---

## ⭕ Zero Values (Значения по умолчанию)

В Go нет понятия "мусора" в объявленной переменной. Если значение не задано, тип инициализируется автоматически:

| Тип | Нулевое значение |
| :--- | :--- |
| `int`, `float`, `byte`, `rune` | `0` |
| `bool` | `false` |
| `string` | `""` (пустая строка) |
| `pointers`, `slices`, `maps`, `channels` | `nil` |

---

## ⚠️ Важные нюансы (Pro Tips для собеса)

1. **Строгая типизация:** Вы не можете сложить `int32` и `int64`. Нужно явное приведение: `sum := int64(a) + b`.
   
2. **Сравнение типов:**
   Сравнивать можно только значения одинаковых типов. `myInt == myInt32` — ошибка компиляции.

3. **Изменение символа в строке:**
   Напрямую нельзя. Нужно: `s -> []rune -> изменить -> string(s)`.
   ```go
   s := "Go"
   r := []rune(s)
   r[0] = 'D'
   s = string(r) // "Do"
