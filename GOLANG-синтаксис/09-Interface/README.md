# 🧩 Интерфейсы в Go

---

## 🗣️ Ответ на собеседовании (кратко и уверенно)

**Интерфейс в Go — это тип, который описывает поведение через набор методов.  
Тип реализует интерфейс неявно, если имеет все его методы.  
Это позволяет писать код, зависящий от поведения, а не от конкретной реализации.**

Дополнительно (по желанию):
> В Go принято использовать маленькие интерфейсы для абстракции и тестирования.

---

## 📜 Что такое интерфейс

Интерфейс — это **набор методов без реализации**.

```go
type Speaker interface {
    Speak() string
}
```
- Интерфейс определяет что должно быть сделано,но не определяет как именно.
## 🧠 Неявная реализация (ключевая особенность Go)
- В Go нет ключевого слова implements.
- Тип реализует интерфейс автоматически, если у него есть все методы интерфейса.


```go
type Human struct{}

func (h Human) Speak() string {
    return "Hello"
}
```
- Human реализует Speaker неявно.

## 📦 Интерфейс — это тип

`Интерфейс можно:`
- передавать в аргументах функций
- возвращать из функций
- хранить в переменных

```go
func Say(s Speaker) {
    fmt.Println(s.Speak())
}
```
- Коду важно поведение, а не конкретный тип.

## 🧺 Пустой интерфейс (interface{} / any)
```go
var x any
```
- не содержит методов
- может хранить значение любого типа
- any — алиас для interface{} (Go 1.18+)
- ⚠️ Минус — потеря типобезопасности.

## 🔎 Type Assertion

- Используется для извлечения значения из интерфейса.
```go
v, ok := x.(string)
```
- `ok == true` → тип совпал
- `ok == false` → другой тип
- без `ok` возможен `panic`

## 🔀 Type Switch

- Используется, если интерфейс может содержать разные типы.
```go
switch v := x.(type) {
case int:
    fmt.Println("int", v)
case string:
    fmt.Println("string", v)
default:
    fmt.Println("unknown")
}
```
- Работает только с интерфейсами.

## ⭕ Nil и интерфейсы (частая ловушка)
```go
var r Reader = (*File)(nil)
```
`r == nil → false`
Причина:
- интерфейс хранит тип + значение
- тип есть → интерфейс не nil

Интерфейс равен `nil`только если и тип, и значение равны `nil`

## 🧱 Маленькие интерфейсы (идиома Go)

# ✔️ Хорошо:
```go
type Writer interface {
    Write([]byte) (int, error)
}
```

# ❌ Плохо:
```go
type BigInterface interface {
    A()
    B()
    C()
    D()
}
```
## 🧭 Когда использовать интерфейсы

✔️ # Подходят:

- несколько реализаций
- тестирование (mock / fake)
- границы пакетов
- расширяемость

 ❌ # Не подходят:

- одна реализация
- «на всякий случай»
- преждевременная абстракция

## ⚠️ Важные нюансы (Pro Tips)

- Интерфейсы описывают поведение, а не данные
- Реализация интерфейса всегда неявная
- any ≠ generics
- Type assertion работает только с интерфейсами
- Интерфейс может быть не nil, даже если значение nil
- В Go предпочитают маленькие интерфейсы

## 🧠 Итог

- Интерфейс = контракт методов
- Тип = реализация поведения
- Интерфейс хранит конкретный тип и значение
  
--- 

⬅️ [гет бэк](../)
