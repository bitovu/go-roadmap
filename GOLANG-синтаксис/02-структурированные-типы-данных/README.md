# Структурированные типы данных в Go

---

## 🗣️ Ответ на собеседовании (кратко и уверенно)

«В Go структурированные типы данных предназначены для хранения и организации
нескольких значений.

К ним относятся:
* **массивы** — коллекции фиксированной длины,
* **срезы** — динамические представления массивов,
* **структуры** — объединение полей разных типов,
* **отображения (map)** — пары ключ–значение,
* **интерфейсы** — описание поведения через методы,
* **каналы** — средство обмена данными между горутинами».

---

## 📦 Массивы (Arrays)

* Имеют **фиксированную длину**, задаваемую при объявлении.
* Все элементы одного типа.
* **Размер массива — часть типа**.

```
var a [3]int b := [3]int{1, 2, 3}
```

---

## 🧩 Срезы (Slices)

* Имеют динамическую длину

* Представляют собой ссылку на массив

* Содержат указатель на данные, длину и ёмкость

* Основной тип коллекций в Go

  ``
  s := []int{1, 2, 3}
s = append(s, 4)``

---

## 🏗️ Структуры (Struct)

* Объединяют поля разных типов

* Используются для описания сущностей предметной области

``
type User struct {   
    Name string
    Age  int
}
``
``
u := User{
    Name: "Alice",
    Age:  25,
}``

---

## 🗺️ Отображения (Maps)

* Хранят данные в формате ключ–значение

* Ключи уникальны

* Порядок элементов не гарантируется

``
m := map[string]int{
    "a": 1,
    "b": 2,
}
``

---

## 🔄Каналы (Channels)

* Используются для обмена данными между горутинами

* Обеспечивают синхронизацию выполнения

  ``
  ch := make(chan int)
  ch <- 10x := <-ch
``

``

---

## ⭕ Zero Values (Значения по умолчанию)
Тип	Zero Value
array	Массив с нулевыми значениями
slice	nil
map	nil
struct	Zero Value у всех полей
interface	nil
channel	nil
⚠️ Важные нюансы (для собеседования)

Массив и срез — разные типы

Массив передаётся по значению, срез — по ссылке

Запись в nil map вызывает panic

Интерфейс может быть не nil при nil значении внутри

Небуферизованные каналы блокируют выполнение
