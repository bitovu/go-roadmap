# Структурированные типы данных в Go

---

## 🗣️ Ответ на собеседовании (кратко и уверенно)

«В Go структурированные типы данных предназначены для хранения и организации
нескольких значений.

К ним относятся:
* **массивы** — коллекции фиксированной длины,
* **срезы** — динамические представления массивов,
* **структуры** — объединение полей разных типов,
* **отображения (map)** — пары ключ–значение,
* **интерфейсы** — описание поведения через методы,
* **каналы** — средство обмена данными между горутинами».

---
## 📦 Массивы (Arrays)

Массивы — это структурированный тип данных, представляющий собой
коллекцию элементов **фиксированной длины**.

* Размер массива задаётся при объявлении и **не может быть изменён**.
* Все элементы массива имеют **один и тот же тип**.
* **Размер массива является частью его типа**.

```go
var a [3]int
b := [3]int{1, 2, 3}
Типы [3]int и [4]int считаются разными и несовместимыми.
```
---

## 🧩 Срезы (Slices)

Срезы — это основной тип коллекций в Go.  
Они представляют собой **динамическое представление массива**.

* Размер среза может **изменяться во время выполнения**.
* Срез содержит:
  * указатель на массив данных
  * длину (`len`)
  * ёмкость (`cap`)
* Несколько срезов могут ссылаться на **один и тот же массив**.

``
s := []int{1, 2, 3}
s = append(s, 4)
``

---

## 🏗️ Структуры (Struct)

* Объединяют поля разных типов

* Используются для описания сущностей предметной области

``
type User struct {   
    Name string
    Age  int
}
``
``
u := User{
    Name: "Alice",
    Age:  25,
}``

---

## 🗺️ Отображения (Maps)

* Хранят данные в формате ключ–значение

* Ключи уникальны

* Порядок элементов не гарантируется

``
m := map[string]int{
    "a": 1,
    "b": 2,
}
``

---

## 🔄Каналы (Channels)

* Используются для обмена данными между горутинами

* Обеспечивают синхронизацию выполнения

  ``ch := make(chan int)
  ch <- 10x := <-ch``

  
---

## ⭕ Zero Values (Значения по умолчанию)

В Go отсутствует понятие «неинициализированной переменной».  
Если значение не задано явно, каждая переменная автоматически получает
**нулевое значение своего типа**.

| Тип | Нулевое значение |
| :--- | :--- |
| `array` | Массив, заполненный нулевыми значениями элементов |
| `slice` | `nil` |
| `map` | `nil` |
| `struct` | Все поля инициализированы своими Zero Values |
| `interface` | `nil` |
| `channel` | `nil` |

---

## ⚠️ Важные нюансы (Pro Tips)

1. **Массив и срез — разные типы**  
   Массив имеет фиксированный размер и передаётся по значению,  
   срез передаётся как структура, указывающая на массив данных.

2. **Запись в `nil map` приводит к panic**  
   Перед использованием `map` необходимо инициализировать через `make`.

3. **Структуры сравнимы не всегда**  
   Структуры можно сравнивать, только если все их поля поддерживают сравнение.

4. **Интерфейс может быть не `nil`**  
   Интерфейс считается `nil` только если и тип, и значение внутри равны `nil`.

5. **Каналы могут блокировать выполнение**  
   Небуферизованный канал блокирует отправку или получение,
   пока вторая сторона не будет готова.

