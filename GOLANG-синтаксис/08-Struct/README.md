# 🏗️ Структуры (Structs) в Go

---

## 🗣️ Ответ на собеседовании

**Структура — это пользовательский тип данных, состоящий из набора именованных полей разных типов и используемый для описания сущностей.**

---

## 📦 Что такое структура

Структуры позволяют:
- объединять связанные данные в одну логическую сущность
- моделировать предметную область
- создавать собственные типы данных

Каждое поле структуры имеет:
- имя
- тип
- значение

---

## 🧱 Объявление структуры

```go
type User struct {
    Name string
    Age  int
}
```

## 🧩 Инициализация структуры
# Именованная инициализация (рекомендуется)
```go
u := User{
    Name: "Alice",
    Age:  25,
}
```

# Позиционная инициализация
```go
u := User{"Alice", 25}
```
⚠️ Не рекомендуется — зависит от порядка полей.

## ⭕ Zero Values

- Если структура не инициализирована явно,все её поля получают нулевые значения своих типов.
```go
var u User
// u.Name == ""
// u.Age  == 0
```

👉 Доступ к полям
```go
u.Name = "Bob"
fmt.Println(u.Age)
```

## 📍 Указатели на структуры

- Структуры в Go передаются по значению.
```go
func Update(u User) {
    u.Age = 30 // изменяет копию
}
```

# Для изменения исходной структуры используется указатель:
```go
func Update(u *User) {
    u.Age = 30
}
```

# Go автоматически разыменовывает указатели:
```go
u := &User{}
u.Name = "Bob" // (*u).Name
```
## 🧩 Методы структур

- Для структур можно определять методы.
```go
func (u User) Greet() string {
    return "Hello, " + u.Name
}
```
`Value receiver vs Pointer receiver`
- Value receiver — работает с копией структуры
- Pointer receiver — может изменять структуру и уменьшает копирование
Рекомендация:
Использовать `pointer receiver`, если метод изменяет состояние или структура большая.

## 🧬 Встраивание (Embedding)

- Go поддерживает композицию через встраивание структур.
```go
type Profile struct {
    Email string
}

type User struct {
    Name string
    Profile
}

u.Profile.Email
u.Email // короткая форма
```

## 🧠 Анонимные структуры

- Можно создавать структуры без имени типа.
```go
u := struct {
    Name string
    Age  int
}{
    Name: "Alice",
    Age:  25,
}
```

# Используются для:
- временных данных
- тестов
- локальных структур

## 🔁 Сравнение структур

- Структуры можно сравнивать оператором ==,если все их поля поддерживают сравнение.

❌ Нельзя сравнивать структуры с полями типов:
- slice
- map
- func

🧭 Когда использовать структуры

✔️ Подходят:

- описание сущностей
- группировка связанных данных
- модели данных
- конфигурации и DTO

❌ Не подходят:

- для описания поведения (используй интерфейсы)

## ⚠️ Важные нюансы (Pro Tips)

- Структуры — основной способ моделирования данных в Go
- Передаются по значению
- Часто используются через указатели
- Поддерживают методы
- Композиция предпочтительнее наследования
- Экспортируемые поля начинаются с заглавной буквы

## 🧠 Итог

- Структура = сущность + данные
- Интерфейс = поведение

---

[гет бэк](../)
